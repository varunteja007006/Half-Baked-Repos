import React from "react";

function DivisionLine() {
  return (
    <>
      <span>{`-`.repeat(60)}</span>
      <span>{` **** `}</span>
      <span>{`-`.repeat(60)}</span>
    </>
  );
}

export default DivisionLine;
