import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Customers',
};

function Page() {
  return (
    <div>
      <p>Customers Page</p>
    </div>
  );
}

export default Page;
